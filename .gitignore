__pycache__/
*.pyc
.DS_Store

# Personal pronunciation dict — managed at runtime by `/tts heal`.
# Ship phonemizer-dict.example.json instead.
phonemizer-dict.json

# Runtime log of words espeak silently dropped.
phonemizer-misses.log

# Audit trail of entries auto-heal added on its own. Derived from the personal
# dict above, so it is personal data too.
phonemizer-autoheal.json
